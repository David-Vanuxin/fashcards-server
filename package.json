{

  "scripts": {
    "dev":"echo '{}' > data.json && nodemon index",
    "start":"echo '{}' > data.json && node index"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
